import React from 'react';
import { BrowserRouter,Routes, Route } from 'react-router-dom';

const Root = () => {
  return (
    <BrowserRouter>
      <Routes>

      </Routes>
    </BrowserRouter>
  );
};

export default Root;